<nav class="navbar navbar-light bg-light ">
  <div class="container-fluid">
    <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>

    <span class="navbar-text">
      <% if user_signed_in? %>
        <p class="nav user">Welcome, <b class="ms-1 me-1"><%= current_user.username %></b> Guru |
          <% if current_user.is_a?(Admin) %>
            <%= link_to t("admin_panel"), admin_tests_path, class: 'text-decoration-none me-2 ms-2' %> |
          <% end %>
          <%= link_to t('feedbacks.feedback'), new_feedback_path, class: 'text-decoration-none me-2 ms-2 '%> |
          <%= link_to t('logout'), destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' },
                      class: 'text-decoration-none ms-2' %>
        </p>
      <% else %>
        <p class="nav user">
          <%= link_to t('login'), new_user_session_path, class: 'text-decoration-none me-1' %> |
          <%= link_to t('signup'), new_user_registration_path, class: 'text-decoration-none ms-1' %>
        </p>
      <% end %>
    </span>

  </div>
</nav>
