<div class="row justify-content-center">
  <div class="col col-md-10">
    <h1><%= t('.header') %></h1>

    <p>
      <%= link_to t('new_badge'), new_admin_badge_path, class: 'text-decoration-none' %>
    </p>

    <%= render 'shared/errors', resource: @badge %>

    <table class="table">
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.icon_url') %></th>
        <th colspan="3"><%= t('.actions') %></th>
      </tr>

      <%= render @badges %>
    </table>
  </div>
</div>
